// secrets.h.example — copy to secrets.h (gitignored) and fill in. NEVER commit secrets.h.
#pragma once
#define WIFI_SSID      "YOUR_SSID"
#define WIFI_PASS      "YOUR_PASS"
#define ALLOY_DATA_URL "https://data.usealloy.ai"
#define ALLOY_API_KEY  "YOUR_ALLOY_API_KEY"   // org key from Alloy dashboard; for a fleet use per-device keys
#define MESH_PATH      "robots/sbr"            // -> uploads/sdk-uploads/robots/sbr/<file>
